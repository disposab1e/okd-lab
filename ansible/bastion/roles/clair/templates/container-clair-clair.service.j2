# container-clair-clair.service
# autogenerated by Podman 1.9.3
# Thu Sep 10 11:58:41 CEST 2020

[Unit]
Description=Podman container-clair-clair.service
Documentation=man:podman-generate-systemd(1)
Wants=network.target
After=container-clair-postgres.service
Requires=container-clair-postgres.service


[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
Restart=on-failure
ExecStartPre=/usr/bin/rm -f %t/%n-pid %t/%n-cid
ExecStart=/usr/bin/podman run \
    --conmon-pidfile %t/%n-pid \
    --cidfile %t/%n-cid \
    --cgroups=no-conmon \
    -d \
    --rm \
    --ip {{ okd_lab_clair_ip }} \
    -e CLAIR_CONF=/clair/config.yaml \
    -e CLAIR_MODE=combo \
    -e SSL_CERT_DIR=/clair \
    --name=clair-clair \
    -v /var/opt/clair/config:/clair:Z \
    {{ okd_lab_quay_clair_image }} -conf /clair/config.yaml -mode combo 
ExecStop=/usr/bin/podman stop --ignore --cidfile %t/%n-cid -t 10
ExecStopPost=/usr/bin/podman rm --ignore -f --cidfile %t/%n-cid
PIDFile=%t/%n-pid
KillMode=none
Type=forking

[Install]
WantedBy=multi-user.target default.target
