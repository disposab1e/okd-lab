---
- name: Git global config user.name
  git_config:
    name: user.name
    scope: global
    value: lab

- name: Git global config user.email
  git_config:
    name: user.email
    scope: global
    value: lab@lab.okd.example.com
